import { PagePlaceholder } from '../../components/PagePlaceholder'

export function PostProcessingTab() {
  return <PagePlaceholder title="Settings → Post-processing — verify, extract, move, phase 5" />
}
